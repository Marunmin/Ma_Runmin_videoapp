<h1>{{title}}</h1>
<br>
<p>{{message}}</p>
<br>
<div>
    <h2>action</h2>
    <ul>
        {{#each data}}
        <li>
            <h3>{{film_name}}</h3>
            <img src="images/{{film_pic}}" alt="{{film_name}}">
            <p>{{film_desc}}</p>
            <video id="myVideo" controls>
                        <source src="videos/{{film_traller}}" type="video/mp4"/>
                        <!-- <source src="video/Rishard-sherman.ogv" type="video/ogg"/>
                        <source src="video/Rishard-sherman.webm" type="video/webm"/> -->
                        Your browser does not support the video tag.
              </video>
        </li>
        {{/each}}
    </ul>
</div>
